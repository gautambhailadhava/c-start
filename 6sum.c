#include <stdio.h>
#include <conio.h>
int main()
{
    int a;
    int b;
    printf("enter the value of a");
    scanf("%d", &a);
    printf("enter the value of b");
    scanf("%d", &b);
    printf("sum of the two number is %d", a + b);
    return 0;
}