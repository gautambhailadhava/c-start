#include <stdio.h>
#include <conio.h>
int main()
{
    int r;
    float area;
    area = (3.14 * r * r);
    printf("enter the value of r  ");
    scanf("%d", &r);
    printf("area of circle is %f %d %d", area);

    return 0;
}